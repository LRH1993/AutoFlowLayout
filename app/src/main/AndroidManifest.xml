<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
          package="com.example.lvruheng.autoflowlayout">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/icon_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/icon_launcher"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name="com.example.lvruheng.autoflowlayout.MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN"/>

                <category android:name="android.intent.category.LAUNCHER"/>
            </intent-filter>
        </activity>
        <activity android:name=".NormalFlowActivity"></activity>
        <activity android:name=".SpecialFlowActivity"></activity>
        <activity android:name=".NormalGridActivity"></activity>
        <activity android:name=".SpecialGridActivity"></activity>
    </application>

</manifest>